#include "test.h"

int main(int argc, char *argv[]) {
    // size_t num_groups;
    // num_groups = pool_num_group_helper(argc, argv);

    // Pool *pool;
    // if(init_pool(&pool, num_groups) != 0) {
    //     return -1;
    // }

    // destroy_pool(pool);
    // pool = NULL;
}